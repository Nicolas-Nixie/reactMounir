import styles from "./Footer.module.css";

const Footer = (props) => {
    return (

        <div class={styles.contaierfooter}>
            <footer>Footer</footer>
        </div>
)}

export default Footer